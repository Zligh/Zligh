<svg viewBox="0 0 80 30" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="glow">
      <feGaussianBlur stdDeviation="1.5" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Dynamic view count only -->
  <text id="viewCount" x="40" y="22" text-anchor="middle" font-family="SFMono-Regular, ui-monospace, monospace" font-size="18" fill="#ff6a00" font-weight="bold" filter="url(#glow)">0</text>

  <!-- Tracking pixel -->
  <image href="https://komarev.com/ghpvc/?username=Zligh" width="1" height="1" x="0" y="0" opacity="0"/>
</svg>